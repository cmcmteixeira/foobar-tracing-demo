{
  bartender : {
    uri: "http://localhost:9000"
    uri: ${?BARTENDER_HOST}
  }

  database {
    host: "jdbc:postgresql://localhost:5432/test"
    host: ${?DATABASE_HOST}

    user: "postgres"
    user: ${?DATABASE_USER}

    password: "password"
    password: ${?DATABASE_PASSWORD}
  }

  http {
    port: 9999
  }
}